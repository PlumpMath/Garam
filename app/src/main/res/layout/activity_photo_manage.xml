<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFF"
    android:orientation="vertical"
    android:gravity="top|center_horizontal"
    tools:context="co.mitsuha.lockscreenlearn.PhotoManageActivity"
    android:weightSum="1">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:layout_marginLeft="30dp"
        android:layout_marginRight="30dp"
        android:src="@drawable/image_info" />

    <EditText
        android:id="@+id/photoEditPassword"
        android:layout_width="315dp"
        android:layout_height="wrap_content"
        android:ems="10"
        android:hint="정답"
        android:inputType="text"
        android:textColor="@android:color/background_dark" />

    <Button
        android:layout_marginTop="7dp"
        android:id="@+id/photoUploadBtn"
        android:text="이미지 업로드"
        android:textColor="#000"
        android:background="@drawable/buttonshape"
        android:layout_width="315dp"
        android:layout_height="wrap_content"
        android:onClick="imageChooseBtnClick"/>

    <Button
        android:id="@+id/photoAddBtn"
        android:layout_marginTop="10dp"
        android:text="추가"
        android:textColor="#000"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:background="@drawable/buttonshape"
        android:onClick="addItemClick"
        />

</LinearLayout>
